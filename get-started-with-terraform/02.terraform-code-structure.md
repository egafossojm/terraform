# Terraform code structure

### 1. Terraform module
- This is a directory that contains one or more config files (.tf)

### 2. main.tf
- The `main.tf` file contains the main configurations for a module.
- When the project becomes bigger, it is better to logically split this file in more files (one for the **variables**, one for the **ouputs**, and so on).